import React from 'react'
import './Header.scss'

const propTypes = {
}

export const Header = () => (
  <header className='header'>
    <h3>Header</h3>
  </header>
)

Header.propTypes = propTypes

export default Header
